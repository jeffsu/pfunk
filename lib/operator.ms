class Operator {
  private {
    var ROOMS = {
      front: null
    }
  }

  function initialize(io) {
    this.io = io;
  }

  function join(room) {
    if (room in rooms) {
    }
  }

}

module.exports = #(io){
  return new Operator(io);
}
